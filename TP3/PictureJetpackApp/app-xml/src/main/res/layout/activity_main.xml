<?xml version="1.0" encoding="utf-8"?>
<!--
    Main screen layout (docs/03_screens.md):
      - Toolbar        : app title + Search button (menu icon)
      - Image grid     : RecyclerView with GridLayoutManager
      - Refresh button : FloatingActionButton (bottom-end)
      - Loading state  : centred ProgressBar
      - Error state    : message + Retry button (shown instead of grid on failure)
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <!-- ── AppBar: Toolbar + Search menu item ───────────────────────────── -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/primary"
        app:elevation="4dp">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:titleCentered="true"
            style="@style/Theme.CatMap.Toolbar"
            app:title="@string/app_name"
            app:titleTextAppearance="@style/TextAppearance.CatMap.ToolbarTitle" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- ── Content area (grid / loading / error) ────────────────────────── -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- Image grid (feature 2: display images) -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingHorizontal="4dp"
            android:paddingTop="4dp"
            android:paddingBottom="80dp"
            android:visibility="gone" />

        <!-- Loading indicator (feature 4: show loading indicator) -->
        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/progressBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top"
            android:max="100"
            app:indicatorColor="@color/primary"
            android:visibility="visible" />

        <!-- Error state (feature 6: show error message) -->
        <LinearLayout
            android:id="@+id/layoutError"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="24dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/textErrorMessage"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="@string/error_loading"
                android:textColor="@color/on_surface"
                android:textSize="16sp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonRetry"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="@string/retry"
                app:backgroundTint="@color/primary" />

        </LinearLayout>

    </FrameLayout>

    <!-- ── Refresh FAB (feature 3: refresh images) ──────────────────────── -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabRefresh"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:contentDescription="@string/refresh"
        app:backgroundTint="@color/primary"
        app:srcCompat="@drawable/ic_refresh"
        app:tint="@color/white" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
